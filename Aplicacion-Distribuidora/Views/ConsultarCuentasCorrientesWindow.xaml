<Window x:Class="Aplicacion_Distribuidora.Views.ConsultarCuentasCorrientesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cuentas Corrientes - Distribuidora Elsa María"
        Height="600" Width="750"
        WindowStartupLocation="CenterScreen">

    <Grid Background="#1E1E2E" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.Row="0"
                   Text="Consultar Cuentas Corrientes"
                   FontSize="24" FontWeight="Bold"
                   Foreground="#CBA6F7"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- Buscador -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="TxtBuscar"
                     Grid.Column="0"
                     Height="40" Padding="10"
                     FontSize="14"
                     Background="#313244"
                     Foreground="#CDD6F4"
                     BorderBrush="#CBA6F7"
                     BorderThickness="1"
                     Margin="0,0,10,0"
                     Text="Buscar por nombre..."/>

            <Button x:Name="BtnBuscar"
                    Grid.Column="1"
                    Content="Buscar"
                    Height="40"
                    FontSize="14"
                    FontWeight="Bold"
                    Background="#CBA6F7"
                    Foreground="#1E1E2E"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="BtnBuscar_Click"/>
        </Grid>

        <!-- Tabla de cuentas corrientes -->
        <DataGrid Grid.Row="2"
                  x:Name="DgCuentas"
                  Background="#313244"
                  Foreground="#CDD6F4"
                  BorderBrush="#CBA6F7"
                  BorderThickness="1"
                  RowBackground="#313244"
                  AlternatingRowBackground="#45475A"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserResizeColumns="True">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#CBA6F7"/>
                    <Setter Property="Foreground" Value="#1E1E2E"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="10,5"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="150"/>
                <DataGridTextColumn Header="Apellido" Binding="{Binding Apellido}" Width="150"/>
                <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}" Width="120"/>
                <DataGridTextColumn Header="Saldo Pendiente" Binding="{Binding Saldo, StringFormat=\{0:C\}}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Total -->
        <TextBlock Grid.Row="3"
                   x:Name="TxtTotalDeuda"
                   Foreground="#F38BA8"
                   FontSize="16"
                   FontWeight="Bold"
                   HorizontalAlignment="Right"
                   Margin="0,10,0,0"/>
    </Grid>
</Window>